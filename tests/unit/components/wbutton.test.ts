import { describe, expect, test } from "vitest"

describe("W-Button", () => {
  test("Test vitest", () => {
    expect(true)
  })
  test("should`t work", async () => {
    expect(Math.sqrt(9)).toBe(2)
  })
})
